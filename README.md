# Minishell - Operating Systems Course Project

Bash like shell, with support for pipes, redirections, background task, process 
management (`bg`, `fg`, `list`, `stop`) and signal handling (ctrl+Z and ctrl+C).

## Run
```bash
make
./minishell
```
